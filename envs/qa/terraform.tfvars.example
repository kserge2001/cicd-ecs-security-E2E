# envs/qa/terraform.tfvars
# export TF_VAR_github_token=ghp_xxx

aws_profile    = "default"
aws_region     = "us-east-1"
github_owner   = "your-github-username"
tfstate_bucket = "REPLACE_WITH_YOUR_TFSTATE_BUCKET"

environment = "qa"
branch      = "qa"
subdomain   = "qa"

# qa is gated: a reviewer must approve the deploy
required_reviewers  = ["your-github-username"]
wait_timer          = 0
prevent_self_review = false # set true for a real team

environment_secrets = {
  APP_ENV_SECRET = "qa-secret"
}

ecr_force_delete = true
